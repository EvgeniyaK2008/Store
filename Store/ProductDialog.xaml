<Window x:Class="Store.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Товар" Height="360" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="Название" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
		<TextBox x:Name="NameBox" Grid.Row="1" Height="32" Padding="8,0" FontSize="13"
                 BorderBrush="#CED4DA" Margin="0,0,0,12"/>

		<TextBlock Grid.Row="2" Text="Цена (₽)" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
		<TextBox x:Name="PriceBox" Grid.Row="3" Height="32" Padding="8,0" FontSize="13"
                 BorderBrush="#CED4DA" Margin="0,0,0,12"/>

		<TextBlock Grid.Row="4" Text="Остаток на складе" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
		<TextBox x:Name="StockBox" Grid.Row="5" Height="32" Padding="8,0" FontSize="13"
                 BorderBrush="#CED4DA" Margin="0,0,0,12"/>

		<TextBlock Grid.Row="6" Text="Описание" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
		<TextBox x:Name="DescBox" Grid.Row="6" Height="60" Padding="8,6" FontSize="13"
                 BorderBrush="#CED4DA" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Margin="0,16,0,4"/>

		<StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Отмена" Width="80" Height="32" Margin="0,0,8,0"
                    Click="Cancel_Click" Background="#F3F4F6" BorderThickness="1"
                    BorderBrush="#CED4DA"/>
			<Button x:Name="SaveBtn" Content="Сохранить" Width="90" Height="32"
                    Click="Save_Click" Background="#5B6AF0" Foreground="White"
                    BorderThickness="0"/>
		</StackPanel>
	</Grid>
</Window>
